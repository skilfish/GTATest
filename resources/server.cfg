# 🌐 Grundkonfiguration
sv_hostname "Promi RP"
rcon_password "deinRconPasswort"
sv_maxclients 128
sv_licenseKey "cfxk_B4SLNHlUSu0Z058w8pu2_3fxg7k"

# 🌍 Netzwerk
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

# 📦 Essentials (OX Core & DB)
ensure oxmysql
ensure ox_lib
ensure ox_core
set mysql_connection_string "user=root;password=;host=localhost;port=3306;database=gtatest"
set onesync_enabled 1


# 📦 Admin-System
ensure ox_admin

# 👤 Charakter & Spawn
ensure char_multichar       # Multicharakter-Auswahl (bei Berechtigung)
ensure char_creator         # Charakter-Erstellung mit UI
ensure illenium-appearance  # ox_appearance-Ersatz
ensure spawn_core           # Letzter Standort speichern / spawnen

# 📦 Inventar & Interaktionen
ensure ox_inventory
ensure ox_target

# ⚙️ Logging (optional Webhooks via ox_admin/log.lua)
# Webhook URLs in resources/[admin]/ox_admin/config.lua
ensure log_utils

# 🔧 Lokale Ressourcen
# ensure your_custom_script

# 🛠 Entwickler
sets locale "de"
setr sv_enforceGameBuild 2699
set sv_projectName "PromiRP"
set sv_projectDesc "Next-Gen RP mit ox_core"

# 🧪 Debugging (entfernen für Live)
# add_ace resource.ox_admin command allow
# add_ace group.admin command allow

# 🔚 Start
add_principal identifier.steam:110000123456789 group.admin
